@model IEnumerable<KanbanProjectMvc.Models.BoardSticker>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StickerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StickerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BoardStickerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BoardStickerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BoardStickerId })
        </td>
    </tr>
}

</table>
